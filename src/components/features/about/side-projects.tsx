import { SectionDescription } from "@/components/shared/section-description.tsx";

export function AboutSideProjects() {
    return (
        <SectionDescription>
            Building tools that make life easier has always been the goal, but overplanning used to get in the way. Now, the focus
            is on a “just ship it” approach — launch, learn, improve. Every project, finished or not, brings valuable experience.
        </SectionDescription>
    );
}
